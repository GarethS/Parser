main () {
    if (counter <= 4 || x > 1) {
        counter = 5;
        counter = 6;
        counter = 6;
        if (x == 2) {
            counter = 4;
            /* don't do this line */
          // d
        } 
        while (d == 3) {
            d = d + 1 + d;
        }
    } else {
        counter = 7;
        x = 7;
        // foo() hasn't been defined yet so can't check number of parameters until
        //  we reach the definition
        foo(1, 2+x, 3);
        q = x;
        moveAbsolute(&x, 2);
        //bar(1,   2);
    }
}	

foo(x, &y, z) {
    y = 3;
    b = x;
}

bar(a, b, c) {
    n = 3;
    b = x;
}